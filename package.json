{
  "name": "starweb-tween",
  "description": "Frame-driven tween manager with easing for the browser",
  "author": "Mason L'Etoile",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/starweb-libs/tween"
  },
  "keywords": ["tween", "animation", "easing", "typescript", "browser"],
  "version": "1.0.0",
  "type": "module",
  "devDependencies": {
    "typescript": "~6.0.2"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "prepare": "tsc -p tsconfig.build.json"
  },
  "files": ["dist", "src"],
  "exports": {
    "./*.js": {
      "types": "./dist/*.d.ts",
      "import": "./dist/*.js"
    }
  }
}
